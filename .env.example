# Copy to .env for local `npm start` (optional). On Render, set this in the *frontend* Web Service only.
# Use your BG API public URL, no trailing slash (e.g. https://your-api.onrender.com).
VISIONX_BG_API=http://127.0.0.1:8000
